#Bubblesort
def bbsort(list):
    for i in range(1,len(list)):
        for j in range(len(list)-i):
            if list[j]>list[j+1]:
                a=list[j]
                list[j]=list[j+1]
                list[j+1]=a
    return list
bbsort([1,43,4,3,7])
