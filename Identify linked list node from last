#Identify linked list node from last
def fromlast(list,n):
    slow=list.head
    fast=list.head
    for i in range(n):
        fast=fast.next
    while fast.next:
        fast=fast.next
        slow=slow.next
    return vars(slow)
fromlast(l,1)
