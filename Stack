#Stack
class stack:
    def __init__(self):
        self.data=[]
    
    def push(self, value):
        self.data.insert(0,value)
        
    def pop(self):
        return self.data.pop(0)
        
    def peek(self):
        if self.data!=[]:
            return self.data[0]
        else:
            return ("Empty Queue")

s=stack()
s.push(5)

print(s.peek())
s.push(10)
s.push(10)
print(vars(s))
s.pop()
s.pop()
s.pop()
s.peek()
